# Features

Create one folder per domain feature (for example `groups`, `expenses`, `settlements`).

Suggested per-feature layout:
- `api/` for endpoint calls
- `components/` for feature-specific UI pieces
- `screens/` for feature screens
- `types.ts` for feature models
